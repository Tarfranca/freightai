# Copy this file to ".env" in the same folder as api.py and fill in your key.
# api.py loads it automatically on startup. Never commit the real .env file.

GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-flash-lite-latest
